.DS_Store
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix

# PII — never commit member data to version control (the build already excludes it from the site)
content/reference/*.csv
content/reference/*Service Sign Up*
